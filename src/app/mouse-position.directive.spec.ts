import { MousePositionDirective } from './mouse-position.directive';

describe('MousePositionDirective', () => {
  it('should create an instance', () => {
    const directive = new MousePositionDirective();
    expect(directive).toBeTruthy();
  });
});
